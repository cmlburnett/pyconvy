Python module that will watch a directory and automatically convert video and audio files based on configuration files in the directories.

See test directory for an example of using pyconvy. In that directory, execute `python3 -m pyconvy` and it will run forever watching for files to convert.
